export interface ILoginForm {
    email: string | null;
    password: string | null;
}

export interface IRegisterForm {
    email: string | null;
    password: string | null;
    username: string | null;
    age: number | null;
}
